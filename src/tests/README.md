/**
 * Test suite for Book Catalog API
 *
 * Contains unit tests for models and schemas, plus integration tests for
 * API endpoints.
 *
 * MIGRATION: The original Python `tests/__init__.py` was an empty package
 * marker file containing only a module-level docstring. Node.js / Jest does
 * not require package initializer files — test files are discovered directly
 * (e.g. via the `testMatch` / `testRegex` patterns in `jest.config.ts`).
 *
 * This file exists solely to preserve the descriptive documentation for the
 * test directory. It contains no executable logic and can safely be deleted
 * if documentation continuity is not required. Consider moving this content
 * into a `src/tests/README.md` instead.
 */

export {};
