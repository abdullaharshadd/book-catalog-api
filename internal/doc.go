// Package internal contains the core implementation of the Book Catalog API,
// a CRUD service for managing books.
//
// Migrated from the Python package initializer (app/__init__.py), which only
// declared package-level metadata (version, author, email) and a docstring.
package internal

// Package-level metadata carried over from the original Python package's
// dunder attributes (__version__, __author__, __email__).
const (
	// Version is the current version of the Book Catalog API.
	Version = "1.0.0"
	// Author is the original author of the project.
	Author = "Abdullah Arshad"
	// Email is the original author's contact email.
	Email = "abdullah.arshad.314@gmail.com"
)
