Looking at the source, I need to understand the migrated schemas. Based on the migration notes, `Validate()` returns a `ValidationError` containing `FieldError`s with `Loc`, `Msg`, `Type`. Let me write table-driven tests that check the validation error messages via `Error()` string matching (the Python tests use substring matching on `str(exc_info.value)`).

{
  "code": "package tests\n\nimport (\n\t\"strings\"\n\t\"testing\"\n\t\"time\"\n\n\t\"bookcatalog/internal\"\n)\n\n// MIGRATION_NOTE: The Python source (tests/test_schemas.py) was a pytest suite\n// that exercised the Pydantic schemas BookCreate, BookUpdate and BookResponse.\n// Pydantic performs validation implicitly during construction and raises\n// ValidationError. In Go the migrated schemas (internal.BookCreate,\n// internal.BookUpdate, internal.BookResponse) are plain structs whose\n// validation is performed by an explicit Validate() method that returns an\n// error (an *internal.ValidationError when invalid). These tests therefore\n// construct the struct, call Validate(), and assert on the returned error.\n//\n// MIGRATION_NOTE: Pydantic's whitespace-stripping / empty-summary-to-None\n// behaviour and its length constraints are assumed to be applied by Validate()\n// (or by Validate mutating the receiver, matching the migrated internal.schemas\n// implementation). The Python tests asserted on substrings of str(ValidationError);\n// the Go equivalent asserts on substrings of err.Error(). If the migrated\n// internal package uses different exact message strings, update the wantErr\n// substrings below to match (see MIGRATION_NOTE on message text).\n\n// helperValidateErr returns the error string from Validate(), or \"\" if valid.\nfunc validateErrString(t *testing.T, v interface{ Validate() error }) string {\n\tt.Helper()\n\tif err := v.Validate(); err != nil {\n\t\treturn err.Error()\n\t}\n\treturn \"\"\n}\n\n// --- BookCreate ---------------------------------------------------------\n\nfunc TestValidBookCreate(t *testing.T) {\n\tb := internal.BookCreate{\n\t\tTitle:         \"Valid Book\",\n\t\tAuthor:        \"Valid Author\",\n\t\tPublishedYear: 2023,\n\t}\n\tsummary := \"A valid book summary\"\n\tb.Summary = &summary\n\n\tif err := b.Validate(); err != nil {\n\t\tt.Fatalf(\"Validate() returned unexpected error: %v\", err)\n\t}\n\tif b.Title != \"Valid Book\" {\n\t\tt.Errorf(\"Title = %q, want %q\", b.Title, \"Valid Book\")\n\t}\n\tif b.Author != \"Valid Author\" {\n\t\tt.Errorf(\"Author = %q, want %q\", b.Author, \"Valid Author\")\n\t}\n\tif b.PublishedYear != 2023 {\n\t\tt.Errorf(\"PublishedYear = %d, want %d\", b.PublishedYear, 2023)\n\t}\n\tif b.Summary == nil || *b.Summary != \"A valid book summary\" {\n\t\tt.Errorf(\"Summary = %v, want %q\", b.Summary, \"A valid book summary\")\n\t}\n}\n\nfunc TestBookCreateWithoutSummary(t *testing.T) {\n\tb := internal.BookCreate{\n\t\tTitle:         \"Book Without Summary\",\n\t\tAuthor:        \"Author\",\n\t\tPublishedYear: 2023,\n\t}\n\n\tif err := b.Validate(); err != nil {\n\t\tt.Fatalf(\"Validate() returned unexpected error: %v\", err)\n\t}\n\tif b.Title != \"Book Without Summary\" {\n\t\tt.Errorf(\"Title = %q, want %q\", b.Title, \"Book Without Summary\")\n\t}\n\tif b.Author != \"Author\" {\n\t\tt.Errorf(\"Author = %q, want %q\", b.Author, \"Author\")\n\t}\n\tif b.PublishedYear != 2023 {\n\t\tt.Errorf(\"PublishedYear = %d, want %d\", b.PublishedYear, 2023)\n\t}\n\tif b.Summary != nil {\n\t\tt.Errorf(\"Summary = %v, want nil\", b.Summary)\n\t}\n}\n\nfunc TestBookCreateStripsWhitespace(t *testing.T) {\n\tsummary := \"  Whitespace summary  \"\n\tb := internal.BookCreate{\n\t\tTitle:         \"  Whitespace Book  \",\n\t\tAuthor:        \"  Whitespace Author  \",\n\t\tPublishedYear: 2023,\n\t\tSummary:       &summary,\n\t}\n\n\tif err := b.Validate(); err != nil {\n\t\tt.Fatalf(\"Validate() returned unexpected error: %v\", err)\n\t}\n\tif b.Title != \"Whitespace Book\" {\n\t\tt.Errorf(\"Title = %q, want %q\", b.Title, \"Whitespace Book\")\n\t}\n\tif b.Author != \"Whitespace Author\" {\n\t\tt.Errorf(\"Author = %q, want %q\", b.Author, \"Whitespace Author\")\n\t}\n\tif b.Summary == nil || *b.Summary != \"Whitespace summary\" {\n\t\tt.Errorf(\"Summary = %v, want %q\", b.Summary, \"Whitespace summary\")\n\t}\n}\n\nfunc TestBookCreateEmptySummaryBecomesNil(t *testing.T) {\n\tsummary := \"   \" // only whitespace\n\tb := internal.BookCreate{\n\t\tTitle:         \"Book\",\n\t\tAuthor:        \"Author\",\n\t\tPublishedYear: 2023,\n\t\tSummary:       &summary,\n\t}\n\n\tif err := b.Validate(); err != nil {\n\t\tt.Fatalf(\"Validate() returned unexpected error: %v\", err)\n\t}\n\tif b.Summary != nil {\n\t\tt.Errorf(\"Summary = %v, want nil\", b.Summary)\n\t}\n}\n\nfunc TestBookCreateMissingRequiredFields(t *testing.T) {\n\t// MIGRATION_NOTE: In Python these were three separate constructions each\n\t// missing a required field. In Go the zero value is used for the missing\n\t// field, and Validate() is expected to report it. Empty title/author are\n\t// caught by the \"cannot be empty\" rules; a zero published_year is caught by\n\t// the year range rule.\n\ttests := []struct {\n\t\tname   string\n\t\tinput  internal.BookCreate\n\t\twant   string\n\t}{\n\t\t{\n\t\t\tname:  \"missing title\",\n\t\t\tinput: internal.BookCreate{Author: \"Author\", PublishedYear: 2023},\n\t\t\twant:  \"title\",\n\t\t},\n\t\t{\n\t\t\tname:  \"missing author\",\n\t\t\tinput: internal.BookCreate{Title: \"Title\", PublishedYear: 2023},\n\t\t\twant:  \"author\",\n\t\t},\n\t\t{\n\t\t\tname:  \"missing published_year\",\n\t\t\tinput: internal.BookCreate{Title: \"Title\", Author: \"Author\"},\n\t\t\twant:  \"published\",\n\t\t},\n\t}\n\n\tfor _, tt := range tests {\n\t\tt.Run(tt.name, func(t *testing.T) {\n\t\t\tinput := tt.input\n\t\t\terr := input.Validate()\n\t\t\tif err == nil {\n\t\t\t\tt.Fatalf(\"Validate() = nil, want error mentioning %q\", tt.want)\n\t\t\t}\n\t\t\tif !strings.Contains(strings.ToLower(err.Error()), tt.want) {\n\t\t\t\tt.Errorf(\"Validate() error = %q, want it to contain %q\", err.Error(), tt.want)\n\t\t\t}\n\t\t})\n\t}\n}\n\nfunc TestBookCreateEmptyTitleValidation(t *testing.T) {\n\ttests := []struct {\n\t\tname  string\n\t\ttitle string\n\t}{\n\t\t{\"empty string\", \"\"},\n\t\t{\"whitespace only\", \"   \"},\n\t}\n\tfor _, tt := range tests {\n\t\tt.Run(tt.name, func(t *testing.T) {\n\t\t\tb := internal.BookCreate{Title: tt.title, Author: \"Author\", PublishedYear: 2023}\n\t\t\tgot := validateErrString(t, &b)\n\t\t\tif !strings.Contains(got, \"Title cannot be empty\") {\n\t\t\t\tt.Errorf(\"error = %q, want it to contain %q\", got, \"Title cannot be empty\")\n\t\t\t}\n\t\t})\n\t}\n}\n\nfunc TestBookCreateEmptyAuthorValidation(t *testing.T) {\n\ttests := []struct {\n\t\tname   string\n\t\tauthor string\n\t}{\n\t\t{\"empty string\", \"\"},\n\t\t{\"whitespace only\", \"   \"},\n\t}\n\tfor _, tt := range tests {\n\t\tt.Run(tt.name, func(t *testing.T) {\n\t\t\tb := internal.BookCreate{Title: \"Title\", Author: tt.author, PublishedYear: 2023}\n\t\t\tgot := validateErrString(t, &b)\n\t\t\tif !strings.Contains(got, \"Author cannot be empty\") {\n\t\t\t\tt.Errorf(\"error = %q, want it to contain %q\", got, \"Author cannot be empty\")\n\t\t\t}\n\t\t})\n\t}\n}\n\nfunc TestBookCreatePublishedYearValidation(t *testing.T) {\n\tcurrentYear := time.Now().Year()\n\n\tt.Run(\"year too early\", func(t *testing.T) {\n\t\tb := internal.BookCreate{Title: \"Title\", Author: \"Author\", PublishedYear: 999}\n\t\tgot := validateErrString(t, &b)\n\t\tif !strings.Contains(got, \"Published year must be after year 1000\") {\n\t\t\tt.Errorf(\"error = %q, want it to contain %q\", got, \"Published year must be after year 1000\")\n\t\t}\n\t})\n\n\tt.Run(\"future year\", func(t *testing.T) {\n\t\tb := internal.BookCreate{Title: \"Title\", Author: \"Author\", PublishedYear: currentYear + 1}\n\t\tgot := validateErrString(t, &b)\n\t\tif !strings.Contains(got, \"cannot be in the future\") {\n\t\t\tt.Errorf(\"error = %q, want it to contain %q\", got, \"cannot be in the future\")\n\t\t}\n\t})\n\n\tt.Run(\"valid minimum edge\", func(t *testing.T) {\n\t\tb := internal.BookCreate{Title: \"Title\", Author: \"Author\", PublishedYear: 1000}\n\t\tif err := b.Validate(); err != nil {\n\t\t\tt.Fatalf(\"Validate() returned unexpected error: %v\", err)\n\t\t}\n\t\tif b.PublishedYear != 1000 {\n\t\t\tt.Errorf(\"PublishedYear = %d, want 1000\", b.PublishedYear)\n\t\t}\n\t})\n\n\tt.Run(\"valid current year edge\", func(t *testing.T) {\n\t\tb := internal.BookCreate{Title: \"Title\", Author: \"Author\", PublishedYear: currentYear}\n\t\tif err := b.Validate(); err != nil {\n\t\t\tt.Fatalf(\"Validate() returned unexpected error: %v\", err)\n\t\t}\n\t\tif b.PublishedYear != currentYear {\n\t\t\tt.Errorf(\"PublishedYear = %d, want %d\", b.PublishedYear, currentYear)\n\t\t}\n\t})\n}\n\nfunc TestBookCreateTitleLengthValidation(t *testing.T) {\n\tb := internal.BookCreate{\n\t\tTitle:         strings.Repeat(\"A\", 256),\n\t\tAuthor:        \"Author\",\n\t\tPublishedYear: 2023,\n\t}\n\tgot := validateErrString(t, &b)\n\tif !strings.Contains(got, \"at most 255 characters\") {\n\t\tt.Errorf(\"error = %q, want it to contain %q\", got, \"at most 255 characters\")\n\t}\n}\n\nfunc TestBookCreateAuthorLengthValidation(t *testing.T) {\n\tb := internal.BookCreate{\n\t\tTitle:         \"Title\",\n\t\tAuthor:        strings.Repeat(\"B\", 256),\n\t\tPublishedYear: 2023,\n\t}\n\tgot := validateErrString(t, &b)\n\tif !strings.Contains(got, \"at most 255 characters\") {\n\t\tt.Errorf(\"error = %q, want it to contain %q\", got, \"at most 255 characters\")\n\t}\n}\n\nfunc TestBookCreateSummaryLengthValidation(t *testing.T) {\n\tlongSummary := strings.Repeat(\"C\", 2001)\n\tb := internal.BookCreate{\n\t\tTitle:         \"Title\",\n\t\tAuthor:        \"Author\",\n\t\tPublishedYear: 2023,\n\t\tSummary:       &longSummary,\n\t}\n\tgot := validateErrString(t, &b)\n\tif !strings.Contains(got, \"at most 2000 characters\") {\n\t\tt.Errorf(\"error = %q, want it to contain %q\", got, \"at most 2000 characters\")\n\t}\n}\n\n// --- BookUpdate ---------------------------------------------------------\n\nfunc TestValidPartialUpdate(t *testing.T) {\n\ttitle := \"Updated Title\"\n\tyear := 2024\n\tu := internal.BookUpdate{\n\t\tTitle:         &title,\n\t\tPublishedYear: &year,\n\t}\n\n\tif err := u.Validate(); err != nil {\n\t\tt.Fatalf(\"Validate() returned unexpected error: %v\", err)\n\t}\n\tif u.Title == nil || *u.Title != \"Updated Title\" {\n\t\tt.Errorf(\"Title = %v, want %q\", u.Title, \"Updated Title\")\n\t}\n\tif u.Author != nil {\n\t\tt.Errorf(\"Author = %v, want nil\", u.Author)\n\t}\n\tif u.PublishedYear == nil || *u.PublishedYear != 2024 {\n\t\tt.Errorf(\"PublishedYear = %v, want 2024\", u.PublishedYear)\n\t}\n\tif u.Summary != nil {\n\t\tt.Errorf(\"Summary = %v, want nil\", u.Summary)\n\t}\n}\n\nfunc TestEmptyUpdate(t *testing.T) {\n\tu := internal.BookUpdate{}\n\n\tif err := u.Validate(); err != nil {\n\t\tt.Fatalf(\"Validate() returned unexpected error: %v\", err)\n\t}\n\tif u.Title != nil {\n\t\tt.Errorf(\"Title = %v, want nil\", u.Title)\n\t}\n\tif u.Author != nil {\n\t\tt.Errorf(\"Author = %v, want nil\", u.Author)\n\t}\n\tif u.PublishedYear != nil {\n\t\tt.Errorf(\"PublishedYear = %v, want nil\", u.PublishedYear)\n\t}\n\tif u.Summary != nil {\n\t\tt.Errorf(\"Summary = %v, want nil\", u.Summary)\n\t}\n}\n\nfunc TestUpdateValidationSameAsCreate(t *testing.T) {\n\tt.Run(\"empty title still fails\", func(t *testing.T) {\n\t\temptyTitle := \"\"\n\t\tu := internal.BookUpdate{Title: &emptyTitle}\n\t\tgot := validateErrString(t, &u)\n\t\tif !strings.Contains(got, \"Title cannot be empty\") {\n\t\t\tt.Errorf(\"error = %q, want it to contain %q\", got, \"Title cannot be empty\")\n\t\t}\n\t})\n\n\tt.Run(\"invalid year still fails\", func(t *testing.T) {\n\t\tyear := 999\n\t\tu := internal.BookUpdate{PublishedYear: &year}\n\t\tgot := validateErrString(t, &u)\n\t\tif !strings.Contains(got, \"Published year must be after year 1000\") {\n\t\t\tt.Errorf(\"error = %q, want it to contain %q\", got, \"Published year must be after year 1000\")\n\t\t}\n\t})\n}\n\n// --- BookResponse -------------------------------------------------------\n\nfunc TestValidBookResponse(t *testing.T) {\n\t// MIGRATION_NOTE: In Pydantic BookResponse was constructed directly and\n\t// validated on construction. The migrated internal.BookResponse is a plain\n\t// struct (typically produced by internal.NewBookResponse from a Book). Here\n\t// we build it directly and assert its fields carry through unchanged.\n\tsummary := \"Response summary\"\n\tb := internal.BookResponse{\n\t\tID:            1,\n\t\tTitle:         \"Response Book\",\n\t\tAuthor:        \"Response Author\",\n\t\tPublishedYear: 2023,\n\t\tSummary:       &summary,\n\t}\n\n\tif b.ID != 1 {\n\t\tt.Errorf(\"ID = %d, want 1\", b.ID)\n\t}\n\tif b.Title != \"Response Book\" {\n\t\tt.Errorf(\"Title = %q, want %q\", b.Title, \"Response Book\")\n\t}\n\tif b.Author != \"Response Author\" {\n\t\tt.Errorf(\"Author = %q, want %q\", b.Author, \"Response Author\")\n\t}\n\tif b.PublishedYear != 2023 {\n\t\tt.Errorf(\"PublishedYear = %d, want 2023\", b.PublishedYear)\n\t}\n\tif b.Summary == nil || *b.Summary != \"Response summary\" {\n\t\tt.Errorf(\"Summary = %v, want %q\", b.Summary, \"Response summary\")\n\t}\n}\n\n// MIGRATION_NOTE: The Python test test_book_response_missing_id asserted that\n// constructing a BookResponse without an id raised a ValidationError because\n// Pydantic treated id as a required field. In Go, BookResponse.ID is an int\n// whose zero value is a perfectly valid (if unusual) value; there is no\n// construction-time validation to trigger. A struct with a missing ID simply\n// has ID == 0. This behaviour has no direct Go equivalent and is therefore not\n// migrated as a failing-validation test; instead we document the zero-value\n// semantics. Flagged for manual review if response id must be non-zero.\nfunc TestBookResponseMissingIDZeroValue(t *testing.T) {\n\tb := internal.BookResponse{\n\t\tTitle:         \"Title\",\n\t\tAuthor:        \"Author\",\n\t\tPublishedYear: 2023,\n\t}\n\tif b.ID != 0 {\n\t\tt.Errorf(\"ID = %d, want zero value 0\", b.ID)\n\t}\n}\n",
  "target_path": "internal/tests/test_schemas.go",
  "notes": [
    "The migrated internal.BookCreate/BookUpdate/BookResponse field types are inferred: required strings/ints as value types, optional fields (summary, and BookUpdate's title/author/published_year) as pointers. Adjust if the actual internal.schemas.go definitions differ.",
    "Validation is invoked explicitly via Validate() returning error; tests assert on err.Error() substrings mirroring Python's str(ValidationError) substring assertions. Exact message strings ('Title cannot be empty', 'at most 255 characters', etc.) must match internal/schemas.go — update substrings if they differ (Case A vs Case B per migration notes).",
    "test_book_response_missing_id could not be migrated as a validation failure: Go's int ID has a valid zero value and BookResponse has no construction-time validation. Replaced with a zero-value assertion and a MIGRATION_NOTE.",
    "For missing-required-field cases, Go zero values are used and Validate() is expected to flag them via existing rules (empty title/author, year range). If internal.Validate does not treat zero PublishedYear as invalid, the 'missing published_year' subcase needs adjustment."
  ],
  "unmigrable_components": [
    {
      "name": "test_book_response_missing_id",
      "reason": "Pydantic treated BookResponse.id as a construction-time required field; Go's int has a valid zero value and the migrated struct has no Validate() enforcing non-zero id.",
      "suggestion": "If a non-zero response id is a real invariant, add a Validate() method to internal.BookResponse (or validate in NewBookResponse) and assert on it; otherwise the zero-value test documents current semantics."
    }
  ],
  "requires_manual_review": true
}